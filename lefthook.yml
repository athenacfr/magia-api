pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx}"
      run: pnpm oxlint --deny-warnings --fix {staged_files}
      stage_fixed: true
    format:
      glob: "*.{ts,tsx}"
      run: pnpm oxfmt --write {staged_files}
      stage_fixed: true
